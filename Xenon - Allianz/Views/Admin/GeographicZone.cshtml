@model IEnumerable<Xenon___Allianz.Models.GeographicZoneModel>

@{
    ViewBag.Title = "GeographicZone";
}

<h2>GeographicZone</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Father)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FatherName)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Father)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FatherName)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.Id }) |
                @Html.ActionLink("Details", "Details", new { id = item.Id }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.Id })
            </td>
        </tr>
    }


</table>

<form action="/Admin/RegisterUserAux">
    <div class="form-group">
        <input type="text" class="form-control" name="Username" placeholder="username">
    </div>
    <div class="form-group">
        <input type="text" class="form-control" name="Status" value="Collaborateur">
    </div>
    <select class="form-control" id="sel1" name="GeographicZone">
        @foreach (var item in Model)
        {
            <option value=@item.Id>@item.Name</option>
        }
    </select>
    <button type="submit" class="btn btn-default">Submit</button>
</form>
<!--
<option>1</option>
        <option>2</option>
        <option>3</option>
        <option>4</option>

-->
